 /// @description Execute Code
vsp=0; //defines vsp as being 0
hsp=0; //defines hsp as being 0
bounce=0.5; // defines bounce is 0.5 unless incremented
drag=0.5; // defines drag as being 0.2
state= ; // defines state as being ps.IDLE
facing= 1; //defines facing as being 1 unless incremented
walk_spd=4; // walk speed set to 0 unless incremented
hp=6; // defines hp as being 6
hp_max=6; //defines hp_max as being 6 also
 